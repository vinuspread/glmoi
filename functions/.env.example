# AdMob OAuth credentials
# Copy this file to .env.admob and fill in the actual values.
# .env.admob is gitignored — never commit the actual values.
# NOTE: Named .env.admob (not .env) to avoid conflict with Firebase Functions reserved env var prefixes.
#
# How to get these values:
#   1. Google Cloud Console -> APIs & Services -> Credentials
#   2. Find the OAuth 2.0 Client ID for this project
#   3. Copy Client ID and Client Secret below

ADMOB_CLIENT_ID=your_client_id_here
ADMOB_CLIENT_SECRET=your_client_secret_here
ADMOB_REDIRECT_URI=http://localhost

# Firebase service account key path (for save-token-to-firestore.js)
# Path to the downloaded service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=../glmoi-prod-service-account.json
FIREBASE_PROJECT_ID=glmoi-prod
